image:
  file: .gitpod.Dockerfile

ports:
  - port: 8080
    onOpen: open-preview
  - port: 3306
    onOpen: ignore

tasks:
  - init: composer install || true
    command: apachectl start && tail -f /dev/null
